#include <bits/stdc++.h>


using namespace std;


#define MAX_N 5000


int main() {
    int N;
    int D[MAX_N], E[MAX_N], F[MAX_N];

    cin >> N;

    for (int i = 0; i < N; i++) {
        cin >> D[i] >> E[i] >> F[i];
    }

    
}